# Registro de decisiones

Diario de decisiones tomadas durante el desarrollo (las de arquitectura "grandes" están en `02-arquitectura/decisiones.md`; aquí va el día a día con la IA).

Formato: fecha · fase · decisión · alternativas · por qué.

## Fase de planificación (previa al código)

| Fecha | Fase | Decisión | Alternativas consideradas | Por qué |
|---|---|---|---|---|
| — | Plan | NestJS + Prisma + SQLite / React + Vite | Express, Fastify, FastAPI / Next.js, Angular | Ver ADR-01/02; estructura modular reconocible y quickstart sin fricción |
| — | Plan | Resolver ambigüedades del enunciado por escrito antes de codificar | Improvisar durante el desarrollo | Las 6 ambigüedades (A1–A6) quedaron decididas y justificadas en `reglas-de-negocio.md`; la IA no improvisa reglas de negocio |
| — | Plan | Desarrollo en 10 fases, una sesión de IA por fase | Una única sesión larga | Contexto limpio por fase → menos deriva; cada fase con criterios de aceptación y verificación propios |

## Durante el desarrollo

<!-- Añadir una fila (o entrada breve) por decisión relevante: -->

| Fecha | Fase | Decisión | Alternativas | Por qué |
|---|---|---|---|---|
| 2026-07-16 | 0 | shadcn init con `-b radix -p nova` (CLI 4.x ya no acepta base color); se aceptan las deps que instala el propio CLI (radix-ui, cva, clsx, tailwind-merge, lucide-react, sonner, next-themes, tw-animate-css, @fontsource-variable/geist) | Fijar shadcn a versión anterior con flag `-b neutral` | Interfaz actual del CLI; sus deps son parte integral de shadcn, no deps nuevas del proyecto |
| 2026-07-16 | 0 | Mantener oxlint + TypeScript 6 del template Vite actual (`pnpm lint` ejecuta oxlint) | Migrar a ESLint | Default del template; se migrará solo si hace falta paridad de reglas con el backend |
| 2026-07-16 | 0 | `pnpm.onlyBuiltDependencies` (prisma, @prisma/client, @prisma/engines) en `backend/package.json` | Ejecutar `pnpm approve-builds` manualmente en cada clon | pnpm 10 bloquea postinstall por defecto; sin ello el schema-engine no se instala y `prisma migrate` fallaría en fase 1. Verificado con `pnpm prisma --version` |
| 2026-07-16 | 0 | Sustituir los flags sueltos del scaffold Nest (`strictNullChecks`, `noImplicitAny`…) por `strict: true` | Dejar los flags del scaffold | Criterio de aceptación de la fase; `strict` los engloba y añade el resto |
| 2026-07-16 | 0 | Tipar `VITE_API_URL` augmentando `ImportMetaEnv` en `src/vite-env.d.ts` | Acceso sin tipar (`any` implícito) | Cumple la prohibición de `any` con TS estricto |
| 2026-07-16 | 1 | Adoptar Prisma 7 actualizando la spec `base-de-datos.md` antes de implementar (spec-first): URL del datasource y seed en `prisma.config.ts`, driver adapter `@prisma/adapter-better-sqlite3` obligatorio en `PrismaClient` (P2038 sin él), `better-sqlite3` añadido a `onlyBuiltDependencies` | Downgrade a Prisma 6 para mantener la spec original | El backend ya traía Prisma 7.8 instalado; actualizar la spec primero evita desviación spec↔código y mantiene el stack en la versión soportada |
| 2026-07-16 | 1 | Generator `prisma-client` (canónico v7) con `moduleFormat="cjs"` e `importFileExtension=""`, emitiendo TS en `src/generated/prisma/` (ignorado por git/eslint/prettier); `db:setup` incluye `prisma generate` | Provider legacy `prisma-client-js`; commitear el cliente generado | El provider v7 emite TypeScript fuente; con extensiones `.js` en imports ts-node fallaba (MODULE_NOT_FOUND) — imports sin extensión funcionan en CJS. Cliente no commiteado → un clon limpio lo regenera en `db:setup` |
| 2026-07-16 | 2 | `CostComponent` modelado como tipo función pura `(input: PricingInput) => TierLine[]`; el motor recibe `components: readonly CostComponent[]` con default `[userTiersComponent]` y `baseCents` = suma de `amountCents` de todas las líneas | Interfaz/clase con método `compute()` | Una función basta en TS puro (ADR-05/07); añadir `StorageComponent` = añadir una función al array (OCP), y derivar el total de las líneas hace imposible desincronizar desglose y base. Test dedicado prueba la extensión con un componente sintético |
| 2026-07-16 | 2 | Verificación con `--testPathPatterns` (plural) en lugar del `--testPathPattern` que indica la fase | Downgrade a Jest 29 | Jest 30 renombró el flag (breaking change); con el flag viejo no encuentra tests. La sección Verificación de `fase-02-motor-pricing.md` queda desactualizada a propósito de registro |
| 2026-07-16 | 2 | Guard fail-fast en `computeUserTierLines`: `activeUsers` no entero lanza `Error` de dominio (hallazgo de auditoría; caso no cubierto por spec, decidido en revisión) | `Math.trunc` defensivo; confiar solo en el DTO | La invariante "dinero solo enteros" se defiende donde vive el dinero (ADR-05); trunc ocultaría el bug y persistiría un snapshot corrupto e inmutable (RN-05). El DTO sigue siendo la validación de usuario (400); el guard detecta bugs internos |
| 2026-07-16 | 3 | Clasificación fiscal como tabla declarativa `TAX_ID_RULES` (patrón + validador + `reason` por regla) recorrida en orden | Cadena if-else por tipo | Añadir un tipo = añadir una entrada; cada `reason` vive junto a su regla y se reutilizará en el mensaje del 400 `TAX_ID_INVALID` |
| 2026-07-16 | 3 | Fixtures adicionales derivados a mano para ramas sin cobertura en la tabla de la spec: NIF especial (`K1234567L`, `M0000000T`, `K1234567A`), CIF con inicial "ambos" (`N0032484H`/`N00324848`), "solo letra" P (`P2818002D`) y "solo dígito" A válido (`A58818501`) | Limitarse a los 17 fixtures de la spec (dejando ramas sin test) | 100 % de ramas exige ejercitar K/L/M y las tres políticas de control del CIF; los casos son aplicación directa de los algoritmos cerrados en la spec, no reglas nuevas |
| 2026-07-16 | 3 | Fuzz determinista con LCG de semilla fija (500 strings) en vez de `Math.random()` | Fuzz aleatorio por ejecución | Reproducible en CI: un fallo siempre se puede reproducir con la misma semilla |
| 2026-07-16 | 4 | `VALIDATION_ERROR` como código fallback en `details[].code` para constraints sin código en la spec (`taxId` no-string, `page`/`limit` fuera de rango) | Derivar códigos nuevos por campo (`TAX_ID_REQUIRED`, `PAGE_RANGE`…) | La spec solo cierra 10 códigos; inventar más viola "prohibido inventar reglas de negocio". Aprobado por Diego en sesión |
| 2026-07-16 | 4 | El 409 `TAX_ID_TAKEN` viaja sin `details`: el código se documenta solo en la descripción Swagger del 409 | Incluir `details` con el código también en el 409 | El contrato global restringe `details` a errores de validación (400); el 409 es inequívoco por status en este endpoint |
| 2026-07-16 | 4 | `test:e2e` invoca jest via `node --experimental-vm-modules` y añade `--runInBand` | Dependencia nueva (cross-env) o mock del cliente Prisma en e2e | El query compiler WASM de Prisma 7 usa `import()` dinámico, bloqueado por la VM de Jest sin el flag; sin deps nuevas. `--runInBand` evita carreras entre suites e2e |
| 2026-07-16 | 4 | `PrismaService` y `prisma.config.ts` leen `DATABASE_URL` de entorno con fallback a `file:./prisma/dev.db` | URL hardcodeada (estado previo); inyectar config de Nest | Habilita la BD SQLite temporal de los e2e sin tocar dev ni añadir infraestructura de config para un solo valor |
| 2026-07-16 | 4 | Búsqueda de clientes ordenada por `createdAt desc` | Sin `orderBy` (orden de inserción no garantizado) | La spec no fija orden; la paginación exige orden determinista. Detalle de implementación, no regla de negocio |
| 2026-07-16 | 4 | Normalización de `countryCode` (trim → mayúsculas) añadida a `validaciones.md` **antes** de aplicarla en el servicio (hallazgo de auditoría) | Rechazar `"es"` con `COUNTRY_UNKNOWN` (comportamiento previo) | `"es"` y `"ES"` son el mismo país ISO-3166; rechazarlo sería fricción sin valor. Spec-first: la regla se cierra en la spec y luego se implementa, decidido por Diego en la auditoría de fase 4 |
| 2026-07-16 | 5 | Campos desconocidos en el body: strip silencioso por `whitelist: true`, sin rechazo | Añadir `forbidNonWhitelisted: true` (400) | La spec no define comportamiento para propiedades extra y el pipe global de fase 4 ya hace strip; cambiarlo sería inventar una regla. Un e2e documenta el strip como contrato |
| 2026-07-16 | 5 | Validación de `POST /simulations` sin códigos semánticos propios: fallback `VALIDATION_ERROR` en todos los constraints del DTO | Derivar códigos por campo (`ACTIVE_USERS_RANGE`…) | `contratos-api.md` no cierra códigos para simulaciones; extiende D-fase-4 (`VALIDATION_ERROR` como fallback), confirmado por Diego en sesión |
| 2026-07-16 | 5 | `GET /customers/:id/simulations` vive en `CustomersController` pero delega en `SimulationsService` (exportado por `SimulationsModule`, importado en `CustomersModule`) | Servicio duplicado en customers; controller propio en simulations con ruta anidada | La ruta pertenece al recurso customers, pero la lógica de simulaciones tiene un solo dueño; el import de módulo es el mecanismo idiomático de Nest para compartirla |
| 2026-07-16 | 6 | Añadir `vitest` como devDependency del frontend con `"test": "vitest run"` | Jest (paridad con backend); dejar el frontend sin runner | El scaffold no traía runner y tanto la fase como la tabla de comandos de `CLAUDE.md` fijan `pnpm test` = vitest; config cero reutilizando `vite.config.ts` |
| 2026-07-16 | 6 | El estado `stale` (error con caché previa) mantiene el selector de divisa operativo con la última tasa conocida y conserva la divisa elegida | Forzar EUR ante cualquier fallo del proveedor | La tabla de la spec solo fuerza EUR en "error sin caché"; con caché dice "se usa la última tasa conocida" + banner con fecha |
| 2026-07-16 | 6 | `CurrencyProvider` degrada a EUR cuando no hay tasa utilizable para la divisa elegida (por tasa, no por status de la query) | Ramificar por el estado de `useExchangeRates` | Cubre con una sola regla proveedor caído sin caché y divisa ausente de la respuesta; imposible mostrar un importe con tasa indefinida |
| 2026-07-16 | 6 | Contexto y hook `useCurrency` extraídos a `providers/currency-context.ts`, separados del componente `CurrencyProvider.tsx` | Todo en un archivo (como los shadcn generados, que dejan warning) | Cumple la regla fast-refresh de oxlint (`only-export-components`) en el código propio; los warnings restantes quedan acotados a los shadcn generados |
| 2026-07-16 | 6 | `formatMoney` con locale fijo `es-ES` | Locale del navegador (`undefined`) | Salida determinista y testeable (NBSP incluido en los asserts); JPY sin decimales lo resuelve `Intl` sin lógica propia. Idioma primario del producto es español |
| 2026-07-16 | 7 | Algoritmo fiscal portado 1:1 desde `backend/src/domain/tax-id/` a `frontend/src/domain/tax-id/` (mismos archivos y mismos 57 tests) | Paquete compartido en el monorepo; validar solo en servidor | Coherente con ADR-08 (réplica de pricing): la spec exige feedback inmediato en cliente y el coste de duplicación se acota con la suite espejo idéntica |
| 2026-07-16 | 7 | Países y planes del alta hardcodeados en `lib/countries.ts` (lista cerrada RN-02 + 3 planes del seed) | Endpoint de catálogo `GET /countries` / `GET /plans` | La fase lo fija explícitamente; el formulario no depende de una llamada extra y el servidor sigue siendo autoridad (`COUNTRY_UNKNOWN`/`PLAN_UNKNOWN` revalidan) |
| 2026-07-16 | 7 | Mapeo de errores de API en el alta: 409 → mensaje fijo en `taxId`; `details` de 400 → `setError` campo a campo; resto → toast destructivo genérico | Mostrar siempre `error.message` del servidor; banner global de error | La spec exige el 409 en el campo `taxId`; los `details` traen `field` y encajan 1:1 con el formulario; el toast cubre fallos no mapeables sin dejar estados en blanco |
| 2026-07-16 | 7 | Botón «Ir al simulador» hace scroll/focus tolerante a `#simulador` (no-op si el ancla no existe todavía) | Ocultar el botón hasta fase 8; renderizar un placeholder del simulador | La fase pide el botón ahora y el simulador llega en fase 8; el no-op evita inventar UI provisional y el cableado queda listo |
| 2026-07-16 | 8 | Añadir `@testing-library/react`, `@testing-library/dom` y `jsdom` como devDependencies, con `environment: 'jsdom'` y `setupFiles` en `vite.config.ts` | Extraer un view-model puro del panel y testear solo la función (sin deps) | La fase exige un «test del componente de proyección» y el frontend no podía renderizar nada en test (los 126 tests previos eran de funciones puras). El view-model puro no habría verificado el render, el `aria-live` ni el cableado JSX. Propuesto y aprobado por Diego antes de tocar archivos |
| 2026-07-16 | 8 | Stub mínimo de `ResizeObserver` en `src/test/setup.ts` | `jsdom-testing-mocks` u otra dependencia de polyfills | El slider de Radix exige `ResizeObserver` al montar y jsdom no lo implementa; los tests afirman sobre valores, no sobre la geometría del thumb. Tres métodos vacíos evitan una dependencia entera |
| 2026-07-16 | 8 | `parseIntegerInput` **rechaza** lo que no sea un entero con separadores de miles (`"1,5"` → `null`) en vez de descartar los caracteres sobrantes | Compactar con `replace(/\D/g, '')` (implementación inicial) | Descartar la coma convertía «1,5 GB» en «15 GB»: un cambio silencioso de magnitud ×10 en un campo que se persiste. Detectado por su propio test antes de integrarse |
| 2026-07-16 | 8 | La proyección se retira (mensaje en vez de tabla) cuando `activeUsers` sale del rango del contrato, reutilizando `simulatorFormSchema.shape.activeUsers` como única fuente del rango | Proyectar igualmente cualquier entero; repetir los límites 1–100.000 en el componente | Proyectar un valor que el servidor rechazaría es engañoso; reusar el schema evita que los límites de `validaciones.md` vivan en dos sitios |
| 2026-07-16 | 8 | Tabla de desglose compartida (`CostBreakdownTable`) entre el histórico (cálculo del servidor) y la proyección (réplica ADR-08), con `CostBreakdownLine` como contrato de props propio | Duplicar la tabla en el simulador; tipar las props con `SimulationTierLine` (api) o `TierLine` (dominio) | Mismo render en ambos lados hace la paridad cliente/servidor estructural y no accidental; un tipo de props propio evita acoplar el componente a `api/types.ts` o a `domain/`, que es la misma razón por la que la fase 6 rechazó unificar esos dos tipos |
| 2026-07-16 | 8 | El toast de error del simulador concatena **todos** los `details[]` del contrato (`join(' · ')`) y cae a `error.message` cuando no hay | Mapear `details` a los campos con `setError` (patrón del alta, fase 7) | La fase 8 exige explícitamente «toast destructivo con mensaje del contrato de error»; mapear a campos sin toast la incumpliría. Hallazgo de la auditoría: la versión inicial usaba `details[0]` y descartaba el resto en silencio |
| 2026-07-17 | 9 | `seed.ts` lee `DATABASE_URL` con el mismo fallback que `prisma.config.ts` y `PrismaService` | Dejar la URL hardcodeada (estado previo) | Los otros dos puntos ya la respetaban: con la variable puesta, `db:setup` migraba una BD y sembraba **otra**, en silencio. Bloqueaba el contenedor, que sirve la BD desde `/data`. Hallazgo de la auditoría de fase 9 |
| 2026-07-17 | 9 | Script `lint:ci` (eslint sin `--fix`, `--max-warnings 0`) es lo que ejecuta el workflow; `pnpm lint` conserva `--fix` para el día a día | Usar `pnpm lint` en CI, como indicaba la fase | Un gate que auto-arregla nunca falla por lo arreglable: no es un gate. Aprobado por Diego antes de tocar archivos |
| 2026-07-17 | 9 | `@playwright/test` como devDependency del frontend + smoke de un solo recorrido (alta → simular → guardar → cambiar divisa) contra la API real | No hacer el opcional de la fase; e2e con más recorridos | Diego pidió no descartar el opcional. Un recorrido cubre el cableado de todo el stack; más recorridos duplicarían lo que ya cubren los 30 e2e de backend y los 161 unit del frontend |
| 2026-07-17 | 9 | El CIF del smoke se deriva buscando el carácter de control contra `validateSpanishTaxId` (fuente de verdad) | Fijar un CIF constante; reimplementar el algoritmo del CIF en el test | El alta rechaza duplicados con 409, así que el identificador debe ser único por ejecución; reimplementar el algoritmo en el test duplicaría una regla de negocio cerrada en la spec |
| 2026-07-17 | 9 | El smoke usa `reuseExistingServer: true` y escribe en la BD de dev (crea un cliente real por pasada) | BD aislada para e2e con `reuseExistingServer: false` | Con `false`, Playwright revienta si ya tienes el backend en el 3000 — que es justo lo que el README te dice que hagas. Es un smoke local, no un gate de CI; la BD de dev se regenera con `pnpm db:setup` |
| 2026-07-17 | 9 | `@vitest/coverage-v8` + umbral 100 % en `vite.config.ts`, acotado a `src/domain/**` | Inferir la cobertura del frontend por paridad con el backend (port 1:1 con suite espejo) | Decidido por Diego: la rúbrica pide cobertura de `domain/`, y «se infiere» no es una medida. El umbral en config impide que se degrade en silencio |
| 2026-07-17 | 9 | Contenedores sobre `node:20-bookworm-slim` con toolchain (`python3 make g++`) y pnpm fijado a `10.31.0` vía `corepack prepare` | `node:20-alpine`; `corepack enable` a secas | `corepack enable` sin pin activa pnpm v11, que no arranca en Node 20 (`ERR_UNKNOWN_BUILTIN_MODULE`); y `better-sqlite3` no resuelve por prebuild aquí, cae a node-gyp y necesita toolchain. Ambas cosas se descubrieron ejecutando el build, no razonando sobre él |
| 2026-07-17 | 9 | Capturas del README en `docs/capturas/` | `ai-workspace/04-proceso/capturas/` | Ese directorio se eliminó a propósito (commit `d29c251`); las capturas son material del README, no del workspace de proceso |

## Checklist final contra la rúbrica  ·  verificado 2026-07-17

- [x] **Validación fiscal: tabla de fixtures en verde (backend y frontend).** Backend: 80 tests de `domain/` (57 fiscales, incluidos los 17 fixtures de la spec y los derivados para K/L/M y las tres políticas de control del CIF). Frontend: suite espejo dentro de los 161 tests. Cobertura de `domain/` medida al **100 % de ramas en ambos lados** (backend: `jest --coverage`; frontend: `pnpm test:cov`, 7 archivos, umbral 100 % fijado en `vite.config.ts`).
- [x] **Tramos: 8 casos dorados en verde en ambos lados.** Tabla idéntica `[1, 10, 11, 15, 50, 51, 100, 200]` en `backend/src/domain/pricing/pricing-engine.spec.ts` y `frontend/src/domain/pricing/pricing.spec.ts` (ADR-08).
- [x] **Persistencia con snapshot verificada releyendo histórico.** 30 e2e de backend + smoke de Playwright (`frontend/e2e/smoke.spec.ts`): guarda una simulación y la relee en el histórico con el mismo desglose (15 usuarios → 169,40 €).
- [x] **Responsive 360 px sin scroll horizontal.** Medido en dashboard, alta y detalle: `scrollWidth === clientWidth` en los tres (360/360, 360/360, 345/345).
- [x] **Estados carga/error/fallback de la API de divisas demostrables.** Los tres reproducidos en vivo bloqueando el proveedor: `loading` (spinner), `stale` con caché → banner «Tipos de cambio del 17/7/2026 (sin conexión con el proveedor)» manteniendo el selector operativo con la última tasa (captura en `docs/capturas/divisas-degradacion.png`) y `unavailable` sin caché → «Sin tipos de cambio: importes en EUR», divisa forzada a EUR y selector deshabilitado.
- [x] **README: clon limpio arranca en ≤ 5 comandos (cronometrado).** `git clone` a una carpeta nueva y sólo los comandos del README: backend `pnpm install` + `pnpm db:setup` = **11 s**, frontend `pnpm install` = **4 s**. API con los 3 clientes demo, Swagger en `/docs` (200) y dashboard sirviendo. Sin pasos no escritos ni `.env` que rellenar. Alternativa `docker compose up --build` verificada por separado (backend healthy, seed sobre el volumen, front sirviendo).
- [x] **CI en verde en GitHub.** Run [29582536523](https://github.com/MrFlackstone/saas-o-matic/actions/runs/29582536523) sobre `main`: Backend (lint · test · e2e) ✓ 2m16s · Frontend (lint · test · build) ✓ 30s. Los únicos avisos son los 2 warnings preexistentes de oxlint en los shadcn generados y el de deprecación del runtime Node 20 de las propias actions (no afecta al `node-version: 20` con el que corren los tests).
